# Documentos originales

`yako-schema.sql` y `yako-dtos.ts` tal como se recibieron, antes de la revisión.

Se conservan solo como referencia histórica; **no son la fuente de verdad**:

- El esquema vigente es `src/db/schema/` (Drizzle) más las migraciones en `drizzle/`.
- Los DTOs vigentes viven en cada módulo de `src/`.

Las diferencias y por qué se hicieron están en [../revision-documentos.md](../revision-documentos.md).
